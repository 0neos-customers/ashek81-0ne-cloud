New “0ne-app” feature: let’s integrate the gohighlevel media manager into the 0ne app. I’d like it to have its own side bar menu item. I would like to sync/mirror the files and directories that GHL has. I don’t want to store anything ourselves but more so use GHL for storage. I DO want to be able to leverage our GHL media storage in other apps like the Skool post scheduler. So instead of having to put a file URL we have a more traditional “upload” functionality that on the backend uploads to GHL media storage and then the app uses that public url rather than me putting its own. Id also like to have a “Bulk Uploader” page where I can see/navigate directories, make new ones if needed, then be able to bulk upload into it. Obviously I should then be able to see it in the library. 